# Branches to checkout
branches=(
"master"
"development"
)

# Output filter (egrep pattern)
filter="*"

# Function to be benchmarked
function run {
    git branch --show-current | tee -a testfile.log
}
